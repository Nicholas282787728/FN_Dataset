How do two computers with the same IP from different networks communicate with each other? How does the routing work?As any other computers on different networks using private ip-addresses. Using NAT(Network Address Translation) or PAT(Port Address Translation). The private network has one or multiple adresses shared on the binding network. On internet this is a public address shared by all hosts on the private network.