Why does Windows still use NTFS? Why not ext4, the file system for Linux, since it actively prevents disk fragmentation?
First of all, you are probably running this benchmark from inside Linux, which has an abysmal NTFS driver. Windows is designed around NTFS, so viewing NTFS performance inside of Linux is like taking an F-1 car in a dirt road. It’s not a fair assessment. Second, NTFS uses a rich set of metadata that allows the fine grain of access control and quota management in the filesystem. If you want that level of control in Linux, you will find yourself off in Linux (Posix) ACL land. Though the Linux purist may rage that traditional user/group/world permissions are enough for everyone, Microsoft needed... (more)