Why does Windows still use NTFS? Why not ext4, the file system for Linux, since it actively prevents disk fragmentation?
NTFS currently actively manages fragmentation, however, this is always a function of the OS, not the file system. NTFS is a fully functional, efficient, modern file system. I cannot think of any feature of the alternative file systems that NTFS does not also support. NTFS is a proprietary file system, and the Unix, Linux, Mac OS and BSD solutions are the resault of poor reverse engineering and implementation. The reasons being that the security paradigm of NTFS is completely different than any Unix based system. So this results in management solutions that are shoehorned into Linux to make it... (more)