How do we add two arrays in java?I will assume that the first array can be greater or equal in size with the second array. firstarray:- fr secondarray:-sr Here it goes:- class add { public static void main(String hj[]) { int firstarray[],secondarray[],finalarray[],i; for(i=0;i<sr.length;i++) { finalarray[i]=fr[i]+sr[i]; } i--; for(;i<fr.length;i++) { finalarray[i]=fr[i]; } for(i=0;i<fr.length;i++) { System.out.println(finalarray[i]); } } } The size of the final array should be of the size of the bigger array i.e. the first array. P.S :-You can further put a check that the first array is bigger in size to avoid exception in the second loop. 91 Views  View Upvotes  Answer requested by Manojkumar Dasari