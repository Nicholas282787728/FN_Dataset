How do I write a program that count the occurrence of each element in an int array of size 25?For C language!Unless you make use of a struct, you will need three arrays; the original 25 element int array, an array to store each unique element and an array for the count of each unique element. Rather than provide the program, I will give some psuedo-code: intArrays inputArray[25], outputArray[25], elementCount[25] uniqueElements = 0 FOR i = 0 to 24 counted = FALSE FOR j = 0 to uniqueElements-1 IF inputArray[i] = outputArray[j] THEN elementCount[j] = elementCount[j] + 1 counted = TRUE exit j-loop ENDIF NEXT J IF NOT counted THEN outputArray[uniqueElements] = inputArray[i] elementCount[uniqueElements] = 1 uniqueElements = uniqueElements + 1 ENDIF NEXT i 189 Views