How do I scan strings into a pointer array?for instance say I have a pointer array for colours and I initialize it as char *colors[]={"red" , "blue" , "yellow" }; what if I don't want to initialize the pointer array in the code itself? Is there some function of a kind like scanf I can use here?There are multiple options depending on your requirement. Most popular would be: char colors[3 /* NUM_OF_COLORS */][10 /* MAX_COLOR_LEN */] = { {0} }; for(i = 0; i < 3; ++i) scanf("%9s", colors[i]); Or, char* get_colors() { char name[100]; scanf("%99s", name); return strdup(name); }  char *colors[3]; for(i = 0; i < 3; ++i) { colors[i] = get_colors(); assert(colors[i] != NULL); } .... /* Your program */ for(i = 2; i >=0; --i) { free(colors[i]); } 101 Views  View Upvotes  Answer requested by Vamshi Aruru