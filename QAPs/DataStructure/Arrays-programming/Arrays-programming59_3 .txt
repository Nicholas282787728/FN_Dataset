How do I open files using array in C?fopen simply expects a C string as the filename. So, by constructing the filename properly, the desired operation can easily be implemented! #include <stdio.h> #include <stdlib.h> #define MAX_SIZE 10000  char* concat(char *s1, char *s2,char *s3) { char *final = malloc(strlen(s1)+strlen(s2)+strlen(s3)+1);//+1 for the null-terminator strcpy(final, s1); strcat(final, s2); strcat(final, s3); return final; }  int main() { FILE *fp; for(int i=1;i<=5;i++) { char numb[MAX_SIZE]; sprintf(numb,"%d",i); char *filename=concat("file",numb,".txt"); fp = fopen (filename, "r");  //Use the file  fclose(fp); } return 0; } 61 Views