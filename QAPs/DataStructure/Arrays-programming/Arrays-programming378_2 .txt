How do you initialize an array of structures in C?Here is a sample program to initialize an array of structures (globally) of the expected format and try to print them in the main. #include<stdio.h>  typedef struct p { char c; float x,y; }point;  point P[2]={{'a',1.0,2.0},{'b',3.0,4.0}};  void main()  { printf("\n %c %f %f",P[0].c,P[0].x,P[0].y); printf("\n %c %f %f",P[1].c,P[1].x,P[1].y); } 251 Views  View Upvotes