What will be the C code if I want to print all the entered elements in reverse order without using arrays?(No arrays to be used for storing and printing)#include <stdio.h> void func() { int num; //Enter 0 to stop or to mark termination. scanf("%d", &num); if(num!=0)  func(); printf("%d\n", num); } int main() { func(); return 0; } In the above code, enter the numbers and 0 to stop the input. void func(int counter) {  if(!counter)  return; int num; scanf("%d", &num); func(counter-1); printf("%d\n", num); } int main() {  int num;  printf("Enter how many numbers to be inputted\n");  scanf("%d", &num); func(num);  return 0; } In this code, user will specify number of iterations. 156 Views  View Upvotes