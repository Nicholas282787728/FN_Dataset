I wrote this program in C to reverse an array of intergers. I get a random number before the reversed array. What's wrong with my code.//try this,u can get the result int main(){ int n; int i=0; // Read size of an array scanf(" %d",&n); // empty array a int a[n]; for(i=0; i<n; i++) { // Initializing array a scanf(" %d", &a[i]); } for(i=n-1; i>=0; i—) { printf("%d\n", a[i]); } return 0;} 3 Answers 72 Views · View Upvotes