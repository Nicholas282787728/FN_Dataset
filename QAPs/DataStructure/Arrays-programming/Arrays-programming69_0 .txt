Why isn’t the address of an array a pointer to pointer in C++?as the compiler correctly explained, the type of the argument is int(*)[5] (pointer to array of 5 int), but the function parameter type is int** (pointer to pointer to int) In order to call the function, the argument type has to be same or convertible to the parameter type void addr(int (*p)[5]); int main() { int hel[5]; addr(&hel); } 150 Views  View Upvotes