I wrote this program in C to reverse an array of intergers. I get a random number before the reversed array. What's wrong with my code.it will work try it #include <stdio.h> int main() {  int n;  int i=0;  // Read size of an array  scanf(" %d",&n);  // empty array a  int a[n];  for(i=0; i<n; i++)  {  // Initializing array a  scanf(" %d", &a[i]);  }  for(i=n-1; i>=0; i--) // edited { printf("%d\n", a[i]); }  return 0; } But this program will not reverse array but it will print array in reversal order. 367 Views  View Upvotes