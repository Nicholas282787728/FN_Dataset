How would one use Arrays.sort on a multidimensional array of ints by the first element of each sub-array in Java?Use a comparator to compare two rows against each other. For example: int[][] array = { {1, 2}, {9, 6}, {4, 7} };  java.util.Arrays.sort(array, new java.util.Comparator<int[]>() { public int compare(int[] a, int[] b) { return (a[0] < b[0] ? -1 : (a[0] == b[0] ? 0 : 1)); } }); 3.7k Views  View Upvotes