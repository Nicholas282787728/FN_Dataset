How do I open files using array in C?$ cat five.c #include <stdio.h> #include <stdlib.h> void main(void) { int filenumber; char filename[1024]; FILE *f; int c;  for(filenumber = 1; filenumber <= 5; filenumber++) { sprintf(filename, "file%d.txt", filenumber); if((f = fopen(filename, "r")) == NULL) fprintf(stderr, "%s: cannot open\n", filename), exit(1); while((c = getc(f)) != EOF) putchar(c); fclose(f); } } $ ./five This is file1.txt This is file2.txt. Different stuff This is file3.txt 33333  This is file4.txt 4 4 4 4 file5 175 Views