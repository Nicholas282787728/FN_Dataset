Why do we use functions rather than arrays?Array An array is the collection of elements of similar data type. Function A function is a body of code that returns a value. Array concept is very different from function concept.Array are used to store variable but the function are used to apply any code on array or more than one array. Array only store variable and elements of different type like-int,float,long. function are the block of code which perform any specific operation on given elements like-single value,more than one value or array. for e.g.:- Array :- #include <stdio.h> int main () { int n[10]; for ( i = 0; i < 10; i++ ) { scanf("%d",&a[i]); } for (i = 0; i < 10; i++ ) { printf("Element[%d] = %d\n", i+1, n[i] ); } return 0; }  Function :- #include <stdio.h> int sum(int arr[], int n) { int sum = 0; int i; for (i = 0; i < n; i++) sum += arr[i]; return sum; } int main() { int arr[10000]; int n, i; printf("Eneter the number of elements in array\n"); scanf("%d", &n); for (i = 0; i < n; i++) scanf("%d", &arr[i]); printf("%d", sum(arr, n)); return 0; }   48 Views