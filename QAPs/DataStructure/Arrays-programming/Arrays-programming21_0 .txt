How do I display top 5 numbers in an array in c++?There are plenty of ways. Here are a few (O(n), of course): #include <iostream> #include <vector> #include <algorithm> #include <functional> #include <random> #include <boost/accumulators/accumulators.hpp> #include <boost/accumulators/statistics/tail.hpp>  int main() { int a[50]; // here's the array // fill it with random numbers from -100 to 100 std::mt19937 g(std::random_device{}()); std::uniform_int_distribution<> d(-100, 100); std::generate(std::begin(a), std::end(a), [&]{return d(g);});  // this is what it looks like: for(int n: a) std::cout << n << ' '; std::cout << '\n'; // #1, the boring way { std::vector<int> m; for(int n: a) { m.insert(std::lower_bound(m.begin(), m.end(), n, std::greater<>{}), n); if(m.size() > 5) m.pop_back(); } std::cout << "The " << 5 << " largest values are:\n"; for(int x: m) std::cout << x << ' '; std::cout << '\n'; } // #2 shooting sparrows with a cannon { using namespace boost::accumulators; accumulator_set<int, features<tag::tail<right>>> acc ( tag::tail<right>::cache_size = 5 ); acc = std::for_each(std::begin(a), std::end(a), acc);  std::cout << "The " << 5 << " largest values are:\n"; for(int x: tail(acc)) std::cout << x << ' '; std::cout << '\n'; } // #3 Introselect (in-place) // bonus: does not waste time sorting the top 5 { std::nth_element(std::begin(a), std::begin(a)+5, std::end(a), std::greater<>{}); std::cout << "The " << 5 << " largest values are:\n"; for(int n = 0; n < 5; ++n) std::cout << a[n] << ' '; std::cout << '\n'; } } live demo Coliru Viewer output in that demo: 91 76 -87 -70 33 60 -87 -30 14 -51 -71 -72 -15 79 97 54 -71 33 -89 51 -53 -73 -60 63 14 9 96 -59 33 64 -82 -30 -17 95 -72 -48 46 -66 47 76 -6 -41 81 96 -52 67 -68 -99 74 -39 The 5 largest values are: 97 96 96 95 91 The 5 largest values are: 97 96 96 95 91 The 5 largest values are: 96 97 95 96 91 5.5k Views  View Upvotes