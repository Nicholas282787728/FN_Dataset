How do I put an integer array inside another array in C?You put the integer array in a structure, and have an array of those structures. (As Kinnear notes, you can't do it directly) # define NCOL 20  typedef struct { int iarray[ NCOL ]; } iarray_struct;  static iarray_struct foo[ 100 ];  void iarray_init( iarray_struct *iarray, int rows ) { int i, j; for( i = 0; i < rows; i++ ) for( j = 0; j < NCOL; j++ ) iarray[i].iarray[j] = i * j; } 310 Views  View Upvotes