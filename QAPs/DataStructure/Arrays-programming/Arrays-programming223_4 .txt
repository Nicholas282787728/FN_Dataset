Can I get some example programs that are done using Array of structures?/* Programs in C for a shop to maintain the total items that the shop deals in */ #include <stdio.h> // Structure declaration that stores the structure elements: name, price and in stock quantity of an article struct packet {  char name[10];  float price;  int in_stock_quantity; }; int main(){ struct packet article[10]; // Array of the type of structure packet declared /* Entering data in the array of structure */ for(i=0;i<10;i++){  printf("\nEnter name of article:");  scanf("%s",article[i].name); // ith index of article's name is being entered  printf("Enter price of article:");  scanf("%f",&article[i].price);  printf("Enter quantity available in stock:");  scanf("%d",&article[i].in_stock_quantity); } printf("\n----------Stock Details-----------\n"); printf("\nName\tPrice\tQuantity\n"); for(i=0;i<10;i++){  printf("\n%s\t%f\t%d",article[i].name, article[i].price, article[i].in_stock_quantity); } } Hope this helps ! You're welcome ;) 153 Views