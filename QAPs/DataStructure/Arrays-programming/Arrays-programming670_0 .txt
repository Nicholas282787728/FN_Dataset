How do you find an array's length or size in C?Originally Answered: C (programming language): How do you find the size of an array in C with the following declaration?The established idiom is to divide the size of the array object by the size of its element: size_t size = sizeof array / sizeof array[0]; (Make sure to do it in the same scope, while the array declaration is visible) 6.9k Views  View Upvotes