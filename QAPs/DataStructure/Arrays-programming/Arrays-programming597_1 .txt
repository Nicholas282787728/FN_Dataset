How does one remove an element from an array in Java?int[] array = {1,2,3,4,5}; if i want to remove index of 1 how to do thisYou can use removeElement method from ArrayUtil. import org.apache.commons.lang.ArrayUtils; then: array = ArrayUtils.removeElement(array, element); Or just copy the array to another array and exclude the index you want excluded. Or use ArrayList (Java Platform SE 7 ) contains remove method to remove an element. 744 Views