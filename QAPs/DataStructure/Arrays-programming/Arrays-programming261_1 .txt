How do I split the given array and return an index such that both sides contain the same number of X and non-X?Hi, Here is a code I did some time before. int function(int X, int[] num){ int length = num.Length; int left = 0; int right = 0; for(int i = 0; i < length; i++){ if(num[i] == X) left++; } for(int i = length-1; i >= 1; i--){ if(left == right) return i; if(num[i] == X) left--; else right++; } return 0; } 412 Views