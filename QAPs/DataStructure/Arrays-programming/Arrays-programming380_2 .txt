How do I check if there are duplicates in the array but the array is very large and had to be distributed across multiple machines?I’d recommend exploring a bloom filter in this scenario. Distribute the array elements into multiple machines using consistent hashing and then layer it with a bloom filter to check if the element may exist. If yes, then follow up with an efficient search to be sure. 46 Views · Answer requested by 1 person