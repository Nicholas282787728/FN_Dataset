How do I find the max value of an array after several updates?It depends on the language. Many languages have a built in function for finding the max value in an array. For example, in PHP you can use <?php // ... code that does all the updates  // Get the max value $max = max($array); ?> PHP: max - Manual Other languages have similar functions. Here are a few Java flavors: Max value in array 260 Views