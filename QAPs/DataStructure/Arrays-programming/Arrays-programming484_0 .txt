How do I divide a Cstring into two arrays C++? (Cstring length is known)If you want to split it on memory level then use a loop or strncpy(): strncpy - C++ Reference Loops have better performance on short cstrings, strncpy is better for large data. If you want to split it just functionally then simply insert '\0': Ideone.com // http://ideone.com/hmCI4Y #include <iostream> using namespace std;  int main() { char arr[] = "hello split"; printf("%s\n", arr); // split size_t halfSize = sizeof(arr)/2; arr[halfSize-1] = '\0'; // or simply arr[5] = 0; char *arr1 = &arr[0]; // or simply = arr char *arr2 = &arr[halfSize]; printf("%s\n", arr1); printf("%s\n", arr2); // your code goes here return 0; } output: hello split hello split 60 Views