How do I write a program that count the occurrence of each element in an int array of size 25?For C language!#include<stdio.h> #include<algorithm> // for algorithm using namespace std; int main() { int i,arr[25],temp,count; for(i=0;i<25;i++) scanf("%d",&arr[i]); sort(arr,arr+25); // this will sort the - you can write your own sort function temp=arr[0]; count=0; for(i=0;i<25;i++) { if(arr[i]==temp) count++; else { printf("occurence of %d = %d times \n",temp,count); count=1; temp=arr[i]; } } printf("occurence of %d = %d times \n",temp,count); // this will be done for //the very last element which will not gets printed cz loop will terminate -- try to //understand it by writing an example on pen paper return 0; } time complexity :o(nlogn) auxiliary space complexity:o(1) 84 Views  View Upvotes