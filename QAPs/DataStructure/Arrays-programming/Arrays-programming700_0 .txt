In Java, why can I create an array of strings but I can not create an array of stacks, and what are some examples?You can create an array of any type, but you should know that that the objects themselves are not instantiated until you use a constructor with them, which must be done with each element - unless you assign an already existing object to them. Maybe that was your confusion. 153 Views  Answer requested by Yu Gu