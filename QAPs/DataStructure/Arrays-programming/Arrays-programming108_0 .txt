How do I create 2D array in C++ which has variable size (VLA) in VS2010?You can use a vector. But as you extend the column of the 2D array don't forget to allocate memory before. You can also allocate memory in your 2D array, in the beginning, if is a at least of size N*M : std::vector< std::vector<int>> vec(N,std::vector<int>(M,1)); Here all the values in your matrix will have the value 1. 87 Views  View Upvotes