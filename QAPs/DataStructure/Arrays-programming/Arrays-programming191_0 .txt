How can I get Array sort index?Thanks for the A2A. Here is the java version.: Map<Integer, Integer> map = new TreeMap<Integer, Integer>();  for (int i = 0; i < array.length; ++i) {  map.put(array[i], i);  }  Collection<Integer> indices = map.values();  System.out.println(indices); 60 Views  Answer requested by Dan Stw