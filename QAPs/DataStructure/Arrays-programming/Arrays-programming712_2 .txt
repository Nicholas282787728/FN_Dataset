What will be the C code if I want to print all the entered elements in reverse order without using arrays?(No arrays to be used for storing and printing)This program will do it using memory allocation: #include <stdio.h> #include <stdlib.h>  int main(){ int len, i; int *mem;  printf("Enter length : "); scanf("%d", &len);  mem = malloc(len * sizeof(int));  for(i=0; i<len; i++){ printf("Enter number %d : ", (i+1)); scanf("%d", mem++); } printf("Reverse order is : \n"); for(i=0; i<len; i++){ printf("%d\n", *(--mem)); }  free(mem); return 0; } 129 Views  View Upvotes  Answer requested by 1 person