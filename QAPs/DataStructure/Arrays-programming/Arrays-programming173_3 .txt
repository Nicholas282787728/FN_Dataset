How can I print all elements of a matrix in diagonal order?i had made a program for my friend long time ago and still it's on laptop so i post it in this answer. #include <stdio.h> int main() {  //declaration section  int storage,j,i;   //executable section  printf("input storage of matrix: ");  scanf("%d", &storage);   int a[storage][storage]; //making array   mat(storage, a, storage); //calling function    for(i=0;i<storage;++i)  {  for(j=0;j<storage;++j)  printf("%d\t", a[i][j]); //printing matrix  printf("\n");  }  return 0; //exit from programm } void mat(int max, int a[max][max], int storage) //function {  //declaration  int i, row, col;  int temp=1;   //executable section  if(storage < 1)  return;    row = col = (max - storage) / 2;   if(storage==1)  {  a[row][col] = temp;  return;  }   for(i=0;i<storage-1;++i)  a[row][col++] = temp++;   for(i=0;i<storage-1;++i)  a[row++][col] = temp++;   for(i=0;i<storage-1;++i)  a[row][col--] = temp++;   for(i=0;i<storage-1;++i)  a[row--][col] = temp++;   mat(max, a, storage-2); //recursion of function  } 172 Views