How do I find the second largest number in an unsorted array using " FOR " loops in turbo c++? Without using arrays.#include <iostream>  using namespace std; int main() { int arr[]={0,5,8,2,9,30,65,65}; int n=8,l1,l2,i; if(arr[0]>arr[1]) { l1=arr[0]; l2=arr[1]; } else { l1=arr[1]; l2=arr[0]; } for(i=2;i<n;++i) { if(arr[i]>l1) { l2=l1; /*l1 always contain largest element from 0 to i-1 elelments, so any element grater than l1 found then after l2 will be equal to previous value of l1*/ l1=arr[i]; } else { if(arr[i]>l2 && arr[i]!=l1) /* any element grater than l2 found it will be the second largest && arr[i]!=l1 is used ,because array contain two 65 if above statement is not present it always return 65. */ l2=arr[i]; } } cout<<"large:"<<l1<<" second :"<<l2; return 0; } 56 Views  Answer requested by Subramanian Narayanan