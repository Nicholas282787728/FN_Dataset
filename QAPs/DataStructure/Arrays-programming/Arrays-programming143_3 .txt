Why do array variables not have an address?Array variable store the address of first element of the array, so in a way the array variable behaves as a pointer, if so, they must have an address but when you print the address of the array variable you get the address of first elements itself. Why so?For doing so you should write &arr[index]. Have a look at http://www.google.co.in/url?q=ht... . 59 Views  Answer requested by Sourabh Khandelwal