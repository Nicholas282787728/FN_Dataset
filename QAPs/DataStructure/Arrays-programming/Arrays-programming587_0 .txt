How do I search for a strings in an array Address book for C++?This strongly depends on the data structure and classes being used. For a simple case where the "Address Book" is just an array of strings, then Kevin Andres has given a good example. If you are using a more complex class, say one that encapsulates multiple different fields (similar to the "Phone Book" in an Android phone or iPhone) then more complexity is needed, but the basic skeleton given by Kevin Andres is still valid and just needs the match logic modified for the most simple cases. (More complex cases might need the iteration logic changed as well, but...) 192 Views