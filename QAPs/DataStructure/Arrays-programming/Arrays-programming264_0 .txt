How do you clone an array that is indexed in an ArrayList of arrays in Java in Sam Hill?It seems like you're not aware that arrays in Java have a clone() method. You can clone every array in an ArrayList as follows: iterate the ArrayList, getting a reference to the array at each position; then call clone() on that. The result of calling clone() is a new array that contains the same elements. 103 Views  Answer requested by Eduardo Dominguez