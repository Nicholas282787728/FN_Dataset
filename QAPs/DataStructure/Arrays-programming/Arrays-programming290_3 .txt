Where are the arrays in C stored, in a stack or a heap?int n; scanf("%d",&n); int arr[n]; In the above code in C, when the progI don't know what C compiler you're using, but in Visual C, that is an error. You cannot dimension a static array with a variable. You need a constant (or a dynamic array). E.g.: #define ARRAY_SIZE 7 int arr[ARRAY_SIZE] or int n; scanf("%d", &n); int *arr = new int [n]; 3.5k Views