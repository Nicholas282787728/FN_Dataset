How do I initialize pointer arrays for 1d char arrays (To store strings) through scanf()/gets() in C?Your solution is not working since your array is pointing to stdin buffer, which will get modified/change on next input. So it may or may not work. Either you have to allocate array at compile time or allocate at runtime. Something like below: #define MAX_NAME_N 100 #define MAX_NAME_LEN 1000 int main() { char *ptr[MAX_NAME_N]; char name[MAX_NAME_LEN]; int i = 0,n = 0; printf("\n Enter N \n"); scanf("%d",&n); if (n > MAX_NAME_N) { printf("\n Too many !!! \n"); return 1; } for(i = 0;i < n;i++) { printf("\n Enter Name \n"); scanf("%s",name); ptr[n] = (char *) malloc(sizeof(char)*(strlen(name)+1)); strcpy(ptr[n],name); } printf("\n Names: \n"); for(i = 0; i < n; i++) { printf("%s\n",ptr[i]); } for(i = 0; i < n; i++) { free(ptr[i]); } return 0; } 63 Views  View Upvotes