Can you graph the ackermann's function?Here is the R-code for the plot options( expressions = 5e5 ) library("scatterplot3d")  ackermann_fn <- function(m, n){ if(m==0) return(n+1) if(m>0 & n==0) return(ackermann_fn(m-1, 1)) if(m>0 & n>0) return(ackermann_fn(m-1, ackermann_fn(m, n-1))) }  ackermann_value <- as.data.frame(cbind(0, 0, ackermann_fn(0,0))) names(ackermann_value) <- c('x', 'y', 'z')  x <- 0:5 y <- 0:5  for(i in x){ for(j in y){ temp <- as.data.frame(cbind(i, j, ackermann_fn(i, j))) names(temp) <- c('x', 'y', 'z') ackermann_value <- rbind(ackermann_value, temp) } }  print(ackermann_value) with(ackermann_value, { s3d <- scatterplot3d(x, y, z, color="red", pch=19, type="h", main="Ackermann Function") s3d.coords <- s3d$xyz.convert(x, y, z) text(s3d.coords$x, s3d.coords$y, labels=z, cex=.5, pos=4) }) My system is not allowing me to go beyond ackermann_fn(5, 5) as the depth of calculations is increasing exponentially. I will try my code on some bigger machine and will update the answer. 171 Views  View Upvotes  Not for Reproduction  Answer requested by Igor Mitrovich