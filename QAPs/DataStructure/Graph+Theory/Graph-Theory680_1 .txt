I want to make a project in which user inputs graph ds and gets output in gui form (webpage or application). What languages can I use to make this?I want to know the language in which i will make my graph as well as the language for taking the nodes as user inputs and representing the result in a webpage/application. I need helpYou have multiple options. But most preferable way is to use JavaScript. Since it has good libraries like d3.js and c3.js to work with SVGs on HTML 5 web page. And other thing you have to consider is the first in which format the application receives the data so you can decide what programming languages to use. 53 Views