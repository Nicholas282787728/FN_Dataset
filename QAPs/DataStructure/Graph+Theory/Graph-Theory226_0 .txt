Why every tree is a graph, but not every graph is a tree?Similar to the reason why every square is a rectangle, but not every rectangle is a square. It's the definition. A tree is a fully-connected directed graph where every node except the root has exactly one parent. But there are plenty of graphs that don't fit this definition. For example, any graph which contains cycles. 346 Views  View Upvotes