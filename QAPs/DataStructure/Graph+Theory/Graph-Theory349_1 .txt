How do you graph (x+1) (x-2) /x(x-3) (x+2)?# call Library-ggplot2 library(ggplot2)  # define equation y <- function(x){return(((x+1)*(x-2)) / (x*(x-3)*(x+2)))}  # plot equation p <- ggplot(data.frame(x = c(-5, 5)), aes(x)) p <- p + stat_function(fun = y, geom = "point", col='red') p <- p + geom_vline(xintercept=-2, col='blue') p <- p + geom_vline(xintercept=3, col='blue') p <- p + geom_vline(xintercept=0, col='blue') p <- p + geom_hline(yintercept=0, col='blue') p <- p + ylab('[(x+1)*(x-2)] / [x*(x-3)*(x+2)]') p 79 Views  View Upvotes