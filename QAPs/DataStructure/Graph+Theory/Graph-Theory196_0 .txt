How do i convert a string to a graph to apply Ford-Fulkerson algorithm for max flow?I need help solving a problem from one of the topcoder practice section. Given a list of string, i need to convert it into a graph structure so that i can apply the max flow. I know the algorithm to convert but i am still unable to write a code for this.//A function to create edges between the strings. you need to pass //the index of the string to src and to. 3rd parameter is the //capacity of the edge which is 1 void createED(int src,int to,int cap) { graph[src].push_back((edge){to,cap,graph[to].size()}); graph[to].push_back((edge){src,0,graph[src].size()-1}); }   int dfs(int v,int t,int f) { if(v==t) return f; visited[v]=true; for(int i=0;i<graph[v].size();i++) { edge &e=graph[v][i]; if(!visited[e.to] && e.cap>0) { int d=dfs(e.to,t,min(f,e.cap)); if(d>0) { e.cap-=d; graph[e.to][e.rev].cap+=d; return d; } } } return 0; }  //function to calculate the max-flow between the source and target //index int calculateflow(int s,int t) { int ans=0; while(1) { for(int i=0;i<2990;i++) visited[i]=false; int temp=dfs(s,t,INT_MAX); if(temp==0) return ans; else ans+=temp; } }  //function to convert a given list of string to a graph void strtograph(string str[],int n) { for(int i=0;i<n;i++) { for(int j=i+1;j<n;j++) { createED(i*n+j,2099,1); createED(j*n+i,2098,1); } } } 56 Views