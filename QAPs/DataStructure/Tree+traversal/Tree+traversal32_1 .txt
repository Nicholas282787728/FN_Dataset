What is the fastest way to perform an inorder traversal of a perfectly balanced binary search tree containing n elements?该网页不存在附加信息...Assuming all the data are distinct, you can do this in time by repeatedly searching from the root for smallest node whose data is greater than the previous node’s data:  1 2 3 4 5 6 7 8 910111213 Node *next(Node *root, int data) {     Node *ret = NULL;     while (root) {         if (root->data > data) {             ret = root;             root = root->left;         } else {             root = root->right;         }     }     return ret; }   