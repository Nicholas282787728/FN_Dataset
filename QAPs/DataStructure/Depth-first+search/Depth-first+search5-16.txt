What are the top 10 algorithms every software engineer should know by heart?
This greatly depends on what you’re working on, but most should know sorting algorithms most used in your industry. If you are a game programmer, these will be different than if you are a quant, a B2B programmer or are working in robotics or operating systems. a couple file traversal and parsing algorithms. almost any industry uses files of data. Even if your primary work doesn’t require it, this will help you build utilities that will make your work faster. a couple heuristic algorithms. These are often not taught in depth in undergraduate programs. An algorithm that produces a result that is... (more)