What are data types?
When we define any data into a computer system, it is primarily identified by what type of data itis. If we are intend to store a number in computer system's memory then the data type is integer, if the number has a decimal part then we store it as a float or double type of data. A word is identified as a string and likewise a single character be it a letter, a special character or a even a number can be identified a "character" data. Now, when we have big amount of homogenous data then we identify what type data is it ? If is a numeric type of data then we hold it into a special group whi... (more)