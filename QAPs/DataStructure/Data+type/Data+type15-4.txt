Is String data type?
Thank for invitation: Of course , you can see primitive datatype like “int ,double ,char ” ,they have no members and functions belonged to them .for example int a = 10; this means a is a data type of int ,and the value is 10,anything else ? no , you can not use a.size() to know the size of it ,because it does not contains anything. But for String there are many functions and attributes in it, it is a class ,you can directly use the like this ,String a = “abc” a.size() to know the sizeof it , and use other functions to know something about it. In addition: in java it is defined like this: public... (more)