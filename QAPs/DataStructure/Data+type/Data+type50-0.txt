Why string is not a primitive data type in Java?
As we know we have some basic or what we call primitive data types in Java such as int, float , char etc. but we have String as non primitive data type. String is group of characters put together in other words its array of char. String in Java is itself is a class and has its own methods to manipulate and operate over object of String class. however Java String differ from primitive data type in terms of its behavior. Strings has its own feature that they are immutable. Primitive data types has limitation that they have fixed size and can hold data of that type only but String can vary in si... (more)