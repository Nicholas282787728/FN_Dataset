What is the difference between data types in c and c++?
There is huge difference between C and C++ in terms of data types , In C language you have struct, enum to define user data types, but In C++ you can use Classes, Sturct, Templates etc. to define user data types, and can also use many inbuilt data structure classes through their objects. Using c++ makes life much more easier, there are many standard templates for many different data types that programmers define their own in c , like stack,queue,map etc. So in c++, You just need to include corresponding header file and use its functionality. Its great let’s take an example #include<iostream> #i... (more)