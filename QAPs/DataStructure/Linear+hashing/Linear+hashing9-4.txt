How do I write a program that, given two sorted arrays of N int values, prints all elements that appear in both arrays, in sorted order?
the following pseudocode should do it.  But, I did this before my morning coffee, so caveat emptor int j = 0; var ArrayOneIndexVal; var ArrayTwoIndexVal; Array ArrayThree; if ArrayOne.length < ArrayTwo.length) ArrayThree = new Array[ArrayOne.length]; ArrayThree = new Array[ArrayTwo.length]; for (i = 0; i++; i<ArrayOne.length) { while (j < ArrayTwo.length && )      if (ArrayOne[i] == ArrayTwo[j])      {           ArrayThree[i] = ArrayOne[i];           break;      } } 