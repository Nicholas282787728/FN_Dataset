How do I write a program that, given two sorted arrays of N int values, prints all elements that appear in both arrays, in sorted order?
WARNING. I made the following language up. Maybe it is a real language. I'm too brain dead after programming all day to figure that out honestly. int func [] (int [] arr1, int [] arr2) {      index1 = 0;      index2 = 0;      int [] newarr = array[len(arr1) + len(arr2)];      while(arr[index1] != NULL) { //Checking to see if end of list. You can also               while(arr[next2] != NULL) { //just find length and iterate until i == len                        if(arr[index1] < arr[index2])                              newarr.append(arr[index1]);                        else                              newarr.append(arr[index2]);               }        }        while(arr[index1] != NULL) //same thing here              newarr.append(arr[index1]);               while(arr[index2] != NULL) //same thing here             newarr.append(arr[index2]);               return newarr; } 