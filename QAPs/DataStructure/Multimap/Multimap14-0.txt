How do I sort array of pair<int, int> in C++ according to the first and the second element?
It is the same as sorting anything else: std::sort (std::begin(a), std::end(a)); Or, pre-c++11 without boost, if a is a C-style array, std::sort(a, a + sizeof a / sizeof *a); Updated 49w ago • View Upvotes