Why do stack variables contain garbage values? Is there any way we can assign our whole stack to NULL?Why do local, stack-based variables have garbage values by default?
Originally Answered: Why will automatic variables contain garbage if it is not set?if you don't initialize a variable after declaring it, it'll certainly contain a garbage value.A garbage value is nothing but value at any random location whose memory has been allocated to that variable.. 