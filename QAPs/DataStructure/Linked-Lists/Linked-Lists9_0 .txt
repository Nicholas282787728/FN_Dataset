Why does linked list is always defined in terms of struct?You can use Malloc to create as many variable as you want or as the programs runs ,but how to keep track of them.?for every new variable you create you also need an extra pointer to keep track of it.the solution to this otherwise tricky problem is to define a struct which has a pointer as one of its component . For understanding more deeply consider the advantages and disadvantages of arrays and linked lists. 56 Views