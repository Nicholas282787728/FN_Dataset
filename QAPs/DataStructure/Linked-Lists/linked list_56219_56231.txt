how to find middle element of link list in one pass ? create a structure with a pointer capable of point to node of link list and with a integer variable which keep count of the number of node in the list . now store the address of the first node of link list in $ start.ptr $ and initialize $ start.count = 1 $ . <br> ensure that the value of $ start.count $ be increment by one after the successfull creation of a new node in link list . similarly , decrement it by one whenever a node be delete from the link list . use $ start.count $ to find the middle element in one single pass . 