How do I implement a C code to insert an element at the middle of the linked list if the size of the list is not known?Traverse the list untill ptr->next!=NULL when you find KEY==ptr->info perform ensert(ptr,KEY); You need a Key element to determine the location where the element to be inserted. Note That ptr point to node which is just before the key node. 112 Views  Answer requested by Swati