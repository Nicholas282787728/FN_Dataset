How do I remove loop from singly linked list?Please tell me how to remove loop from singly linked list in a very simple terms. I need to do it in JavaScript.If you want remove loop in linked list then you can go for recursion.  A function to find an element in the list should contain A check whether it is the last element. If yes then return back if item found otherwise null. If the element is found return that Call the same function with next item. 247 Views