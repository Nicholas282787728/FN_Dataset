Does deleting the head node of a linked list result in a memory lea or is the list eligible for garbage collection?What basically happens when you delete a node is that the memory is released. Now new processes or data storage CAN be done in that memory block. However if you are lucky enough and that memory location is stored in the next block of the linked list you can recollect the data. 130 Views