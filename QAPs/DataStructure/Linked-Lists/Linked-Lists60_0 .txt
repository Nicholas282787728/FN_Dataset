How do I calculate the length of the longest palindrome in a linked list?What I have here is a really abstract algorithm which should do this : int GetLongestPalindromeLength (List l)  declare List palindromeStrList  1. for each list item k in l  1.1 if (isPalindrome (l.Value.ToString() )  1.1.1 palindromeStrList.Add(I.Value.ToString(), I.Value.ToString().Length);  2. return MaxInt(palindromeStrList); Just to be on the safe side, make the code re-entrant. 161 Views