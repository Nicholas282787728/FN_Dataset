it be possible to implement insertion sort for sort link list ? this question be fully answer by the <a href="https://en.wikipedia.org/wiki/insertion_sort" rel="nofollow"> wikipedium page on insertion sort </a> . in summary , yes , you can implement insertion sort on a link list with the same efficiency as for a array because insertion sort only make sequential access to the datum be sort . every time it access the datum , it s look at `` this element '' , `` the next\/previous element '' or `` that element i be look at a few moment ago and whose index\/position in the list i already store . '' 