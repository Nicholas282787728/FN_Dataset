How do I implement a C program that uses a linked list to delete the last node which has the value "x"?struct node *temp,*last,*start; temp=start; /*start is the pointer which points to first node*/  last=start;  while(temp->ptr!=NULL)  {  last=temp;  temp=temp->ptr;  }  last->ptr=NULL;  last= start;  while(last->ptr!=NULL)  {  printf("%d-->",last->info);  last=last->ptr;  }  printf("d-->end of list",last->info); 201 Views