How do I reverse a doubly linked list?You can refer the following code to reverse it: void reverse() { if(head==NULL) { cout<<"\nEmpty List"; return; } Node *temp,*p; temp=head; p=head->prev; while(temp!=p) { swap(temp->data,p->data); temp=temp->next; if(temp==p) return; p=p->prev; }  } Get more about Double Circular linked list at link below: @https://omkarnathsingh.wordpress... 424 Views  View Upvotes