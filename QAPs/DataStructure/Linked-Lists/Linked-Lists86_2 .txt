Is a linked list a self-referencing pointer?A Self Referencing Pointer - #include <stdio.h>  int main(){ void *ptr; ptr=&ptr; return 0; } A pointer variable whose value is the address of the same pointer variable. The above code is a valid syntax. ** Its not the case with Linked List & not even with Circular Linked List with One Element. Circular Linked List with One Element is a Self-Referencing Data Structure. 173 Views  View Upvotes