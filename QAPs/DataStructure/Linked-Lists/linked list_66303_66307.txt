read clr analysis of hash with chain i assume that you be talk about the hash-table that use a adjacency list for the element that have the same hash key -lrb- hash table use chain -rrb- . let s look at a very simple example . we have a hash table such as the following : if you look at key 1 and 3 , which one be more likely to be visit ? the answer be 1 , because it have more element than all other list . therefore , we be more likely to find the mysterious element in 1 s link list . 