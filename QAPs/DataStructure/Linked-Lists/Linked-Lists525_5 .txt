How do I insert a node in a linked list in C++?Explain very very simply.The simplest way to insert at the beginning of the list if head is given then void insertAtBegin(Head,elm) { //Please ignore syntax mistakes //Creating a New node newptr = malloc(sizeof(node)); newptr->data = elm; nerptr->next = NULL;  //Inserting at Begining if(Head==NULL) Head = newptr; else { newptr->next = Head; Head = newptr; } }  If you want to add the node at last then iterate through list void insertAtEnd(Head,elm) { if(Head == NULL) { newptr = createNode(elm); Head = newptr; } else if(Head->next == NULL) { newptr = createNode(elm); Head->next = newptr; } else insertAtEnd(Head->next,elm); } and for in Between You have to Handle several Cases. 1.2k Views  View Upvotes