Given a number in the form of a linked list and a single-digit integer, multiply the result in place?If you are allowed to store the number in another variable Find the number of nodes in linked list to find the place value. Traverse the list - multiply each number in the node to its place value, store In a variable, and add as you traverse the list. Multiply the final variable with the single digit integer. 257 Views