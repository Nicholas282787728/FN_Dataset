how to find a superstar in linear time ? isn t this the <a href="http://webcourse.cs.technion.ac.il/234247/spring2007/ho/wcfiles/extra%20non-obligatory%20self%20reading%20material%20-%20the%20celebrity%20problem.pdf"> celebrity problem </a> ? there will only be one superstar -lrb- celebrity -rrb- if there be one . we use the adjacency matrix representation , where $ a -lsb- i , j -rsb- = 1 $ if there be a directed edge from $ i $ to $ j $ , otherwise it be $ 0 $ . -lrb- i be guess that be allow -rrb- . by look at $ a -lsb- i , j -rsb- $ -lrb- can be do in $ \ mathcal -lcb- o -rcb- -lrb- 1 -rrb- $ time -rrb- we can eliminate at least one of they as a candidate for be the celebrity : if $ a -lsb- i , j -rsb- = 1 $ , then you can eliminate $ i $ . if $ a -lsb- i , j -rsb- = 0 $ we can eliminate $ j $ . maintain a list of current candidate , eliminate one by one . a link list should suffice . at the end , you can verify if you candidate be indeed a superstar . this algorithm will be $ \ mathcal -lcb- o -rcb- -lrb- n -rrb- $ . 