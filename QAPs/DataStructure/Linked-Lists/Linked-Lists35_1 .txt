What is linked list?Linked list is a type of dta structure , just like array is, but its different from array. 1. Data is not stored in contiguous memory space, rather distributed. 2. The linked list has 2 parts - a)stores data b) stores pointer(which links to the next memory space). 3. Needs more space as it stores pointer with data as well. For detailed study refer to lipshutz by shaum series. 58 Views  View Upvotes