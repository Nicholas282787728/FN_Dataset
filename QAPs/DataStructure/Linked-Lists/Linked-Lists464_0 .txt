What is a real life situation where a single linked list can be used to represent data?You can (not to be confused with "should") use them anywhere you can have a set of items that are in order. A linked list would be a good candidate to implement a stack, because you don't need to access any items except the first. Pushing an item on would be cheap and popping an item off would be cheap. 899 Views