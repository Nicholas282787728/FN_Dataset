I'm working with linked lists, but I seem to have forgotten a lot! I can't get it to work. How do I write C++ code to create a list of 20 nodes?I need to create a list of 20 nodes that have a number value in them. I randomly generate the number value for each. I can't get my head around it. I try to compile the code and get all sorts of errors.The code: numberList.h #include <iostream> #include <cstddef> using namespace std; class NumberList { private:  struct ListNode  {  int value;  ListNode *next;  };  ListNode *head; public:  NumberList();  ~NumberList(); void buildSortedList();  void insertNode(int); void searchList(int num); void removeValue(int num); void rotateByTwo(ListNode *head);  bool isEmpty(); void splitList();  void displayListBackwards(); }; numberList.cpp #include <iostream> #include <cstddef> #include <iomanip> #include <cstdlib> #include <ctime> #include "numberList.h" NumberList::NumberList(){  head = NULL; // ensure you do not have a "dangling pointer" } void NumberList::buildSortedList(){  if (head != NULL) { // if there is a list that must be replaced  //~NumberList() // clear up all memory before creating new list  }  srand(time(NULL)); // for random number generation  int randomNum; // variable to populate list with random numbers  randomNum = (rand()%10) + 1;   head->value = randomNum;  head->next = NULL;   ListNode *p; // pointer to expand the list, creating new struct nodes   p = head; // make p point at the beginning (head) of the list   for (int i = 0; i < 19; ++i) { // iterate 19 times to create list of 20  if (p != 0) {// if pointer not pointing outside of the list in memory  while (p->next != 0)  p = p->next; // continue down the list until you get to the end  }   randomNum = (rand()%10) + 1; // generate random number value  p->next = new ListNode; // create new node  p = p->next; // poiont to new node  p->next = NULL; // ensure you do not have a "dangling pointer"  p->value = randomNum; // populate node with random value  }  } } driver: #include <iostream> #include <iomanip> #include <cstdlib> #include "numberList.h" using namespace std; int main() { NumberList list; list.buildSortedList();  return 0; } 181 Views