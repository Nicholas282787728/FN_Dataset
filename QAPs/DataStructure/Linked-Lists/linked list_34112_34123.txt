how can i keep track of the state of a sequence after rotate part of it multiple time ? the easiest option be to use <a href="http://en.wikipedia.org/wiki/skip_list" rel="nofollow"> skip list </a> which will give you much better average case complexity . it will be $ o -lrb- k \ log n -rrb- $ from what the wiki page say : skip list will give you $ o -lrb- \ log n -rrb- $ when you query for a item in the average case . similarly , it give you $ o -lrb- \ log n -rrb- $ in insertion and removal in the average case . together , this mean that the average cost for you operation be $ o -lrb- \ log n -rrb- $ when you use array , the average cost for you operation be $ o -lrb- n -rrb- $ because you have the same probability of $ 1\/n $ for the cost of $ 0 , 1 , 2 , . . n-1 $ . the cost all sum to $ o -lrb- n ^ 2 -rrb- $ divide by $ n $ give you $ o -lrb- n -rrb- $ . when you use link list with the addition of pointer array to all value , the argument be similar . 