How do you reverse a linked list?Any language is fine; feel free to add multiple answers for multiple languagesC struct node{ void *data; struct node *next; }; Iterative: usually faster. int reverse_list(struct node **head) // **head, so you can update it; { if (!*head) // validate input return 0; struct node *tmp = *head; struct node *prev = NULL; while (tmp) { struct node *next = tmp->next; tmp->next = prev; prev = tmp; tmp = next; } *head = prev; // update the head; } 769 Views  View Upvotes