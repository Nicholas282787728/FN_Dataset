In Java, why do we use the LinkedList constructor for declaring Stacks?You don’t. There is a separate Stack class. You probably meant Queue, which LinkedList implements. The reason for this is a Queue is best implemented by a linked list, as the operations required are deletion from the head and addition to the tail, both of which are O(1) with a Linked List. 132 Views · View Upvotes