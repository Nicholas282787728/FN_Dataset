To check the linked list is circular what will be the loop condition? I know further process like taking two pointers. Please suggest me.this loop condition might help: void traverse(node head) { node temp=head; // assuming head is the first node and not the dummy node temp2=head; int flag=0;  while(flag==0 && temp!=NULL) { temp=temp->next; if(temp2==temp) flag=1; }  if(flag==1) cout<<"circular"; else cout<<"linear"; }  354 Views  View Upvotes