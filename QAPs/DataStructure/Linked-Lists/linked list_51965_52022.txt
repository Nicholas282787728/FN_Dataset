average cost threshold protocol with minimum threshold : how to find the price ? okay , so this be how far i get . add each agent be $ \ mathcal o -lrb- n -rrb- $ , for a total of $ \ mathcal o -lrb- n ^ 2 -rrb- $ , but spread out -lrb- the application i have in mind would involve agent come one at a time -rrb- . if you have no agent , start with a empty link list . if you have $ n $ agent , you will already have a solution of $ p _ -lcb- old -rcb- $ for $ n-1 $ agent , as well as a old link list , as well as we new agent , agent $ i $ . the link list have element of the form $ -lrb- \ delta p_j , m_j , m_j -rrb- $ , where $ \ delta p_j = p - f -lrb- m_j -rrb- $ , a.k.a. , how much more money be need if $ p = m_j $ . it be sort by $ m_j $ , smallest first . so we traverse we link list , start with $ -lrb- \ delta p_j , m_j , m_j -rrb- $ . if $ m_i \ ge m_j $ , decrease $ \ delta p_j $ accordingly . if $ \ delta p_j = 0 $ , $ p = m_j $ , then the rest of the link list be discard -lrb- include agent $ j $ s node -rrb- , and we end . if $ \ delta p_j \ gt 0 $ , continue traverse . if $ m_i < m_j $ , keep a reference to $ j $ s node . now traverse the rest of the list to determine $ \ delta p_i $ . if $ \ delta p_i = 0 $ , then $ p = m_i $ , and throw out all node after and include agent $ j $ . if $ \ delta p_i \ gt 0 $ , then before $ j $ insert a node $ -lrb- \ delta p_i , m_i , m_i -rrb- $ , and $ p =p _ -lcb- old -rcb- $ . 