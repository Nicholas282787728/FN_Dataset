How do I add an element in a circular linked list in ascending order in Java?Soo far I have done this algorithm and it is going Infinite Loop public class Node { public int element; public Node next; } public class List { void insert(int e) {  Node nNode = new Node();  Node tNode = head;  nNode.element = e; if (head == null)  head = nNode;  else if (head.element > e) {  nNode.next = head;  head=nNode;  } else { Node pNode = head; while (tNode.next != head && tNode.element <= e) { pNode = tNode; tNode = tNode.next; } pNode.next = nNode;  nNode.next = tNode; tNode.next=head;  } }} 526 Views