hash table collision : why use a link list if we can use a hash set ? it would be pointless to use a <code> hashset </code> . if a number of object land in the same bucket it be because they hash to the same value -lrb- <code> mod nbucket </code> but likely the same actual value -rrb- . what hash value would you use for the inner hash-set ? you be in danger of force the inner <code> hashset </code> to also collide . 