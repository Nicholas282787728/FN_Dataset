In layman's terms, what is the difference between a bubble sort and an insert sort?First, check out these great gifs from Wikipedia: Insertion Sort Animation: Bubble Sort Animation: Basically, it seems that the main advantage of insertion sort is that it's much faster at sorting lists which are mostly sorted (insertion sort will place the value more-or-less where it needs to go rather than moving it over by one as the bubble sort would). 