Why i am getting "Runtime Error(SIGSEGV)" in codechef, while it works perfectly in my local system?
You have to allocate size for a valid size only. Keep that arr[n] after reading n value . Best choice is to declare array globally with the upper bound of the constraints for n      As the problem  executes on how fast you input the values, Do not declare an array ,instead use a single variable . Use fast I/O methods to read input    #include<stdio.h>int main() { int n,k;    int val,count=0;    scanint(n); //faster I/O method see below link for code  scanint(k):    for(i=0;i<n;i++)    {        scanint(val);      if(val%k==0) count++;    }    printf("%d\n",count);   } Link for fast I/O  Really Fast I/O methods for Programming in C++ (Input Methods).. Hope this helps :) 