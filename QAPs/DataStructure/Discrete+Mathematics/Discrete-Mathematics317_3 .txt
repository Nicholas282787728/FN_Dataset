What are the examples of recursion algorithms?Mergesort is a good example. The way to think about it is this. If you want to sort an array, sort the first half, then sort the second half, and then merge the two sorted halves. And that's it. Here's the important part of the code:  public static void mergeSort( double[] b )  {  //mergeSort uses a temporary array to do the merging.  //By creating it here and passing to the other methods,  //we save time by only creating it once.  double[] tempArray = new double[b.length];  //here we call the private mergeSort  mergeSort( b, tempArray, 0, b.length - 1 );  }  /////////////////////////////////////////////////////////////////////////  private static void mergeSort( double[] b, double[] tempArray, int left, int right )  {  if ( left + CUTOFF > right )  {  selectionSort( b, left, right );  return;  }  int mid = ( left + right ) / 2;  mergeSort( b, tempArray, left, mid );  mergeSort( b, tempArray, mid + 1, right );  merge( b, tempArray, left, mid + 1, right );  } // end mergeSort( ) 193 Views