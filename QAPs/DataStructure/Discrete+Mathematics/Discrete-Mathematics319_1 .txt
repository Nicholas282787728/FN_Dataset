How can you use recursion in a program to solve the Tower of Hanoi?In pseudocode, this looks like the following. At the top level, we'll call MoveTower with disk=5 5 , source=A A , dest=B B , and spare=C C . MoveTower(5,A,B,C) FUNCTION MoveTower(disk, source, dest, spare): IF disk == 0, THEN: move disk from source to dest ELSE: MoveTower(disk - 1, source, spare, dest) // Step 1 above move disk from source to dest // Step 2 above MoveTower(disk - 1, spare, dest, source) // Step 3 above END IF 73 Views  View Upvotes