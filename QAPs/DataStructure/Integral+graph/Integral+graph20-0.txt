Which E-Commerce shopping cart solutions have Open Graph Protocol integration?
Magento: Facebook Social plugins (with Facebook Like button) : http://www.magentocommerce.com/e... -- Note that it's a paid extension (19€). Drupal Commerce, being based on Drupal, will play well with any Drupal Open Graph Protocol plugin. See http://www.drupalcommerce.org/ Note that it should be pretty straightforward to implement OGP into any modern E-Commerce platform (Basically, it all boils down to adding a few meta tags to your page). Because it's only about metadata at the page level (not at a more granular level), it's probably way simpler than implementing RDF or GoodRelations, for example. 