Does PHP have any hash map implementation where search for an element takes O(1) time?
Yup, an array. An "array" in PHP isn't a fixed-length data structure like it is in other languages like C.  It's an ordered map, which can be used like a numerically-indexed vector, or a a hashmap. The PHP docs explain it pretty well: http://us2.php.net/manual/en/lan... 