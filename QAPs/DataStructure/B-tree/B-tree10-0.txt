What is B* Tree in Data Structure?
Don’t be confused between Binary tree and B-tree.They both are different. In a binary search tree AVL tree,red black tree etc. , every node can have only one value(key) and maximum of two children.But a B-tree , which can have more than one value(key) and more than two children. B-tree is a self balanced search tree with multiple keys in every node and more than two children for every node. But number of keys in a node and number of children for a node is depend on the order of the B-Tree. Three operations are performed in a B-Tree - Search Insertion... (more)