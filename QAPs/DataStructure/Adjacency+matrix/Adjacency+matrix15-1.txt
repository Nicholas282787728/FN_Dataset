How do I print adjacency matrix of a graph dynamically?For example, a graph has three nodes. If user inputs two data types (for example, some structure elements), the format of adjacency matrix is 2  . If user inputs three data types, format should be 3  . Thanks for replies.
