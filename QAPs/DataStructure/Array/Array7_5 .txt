What is the advantage of using zero-length arrays in C?Great answers. One other use-case I would like to present here. Consider a case where you would like to access and control shared memory segment. The control structure can be implemented as a header to the shared memory segment. I hope the code is self explanatory after reading other answers.  1 2 3 4 5 6 7 8 9101112131415161718192021222324252627282930313233343536373839404142 struct shm_status {  unsigned char init; // initialization state of mem array  unsigned char mod; // modified  unsigned short ownerId; // who owns it  unsigned char mem[]; // user shared memory content begins here }; ... void* get_shm(unsigned int usermem_size) {  unsigned int size = usermem_size + sizeof(shm_status);  key_t key_mem = ftok(...); // unique key for shm  if (key_mem == -1)  {  FATAL(...);  }  //create shm  shmid id_mem = shmget(key_mem, size, IPC_CREAT | IPC_EXCL | DEFAULT_SHARED_MEM_ACCESS);  if ( id_mem == -1)  {  // already exists, connect  id_mem = shmget(key_mem, size, 0);  if (id_mem == -1)  {  FATAL(...);  }  }  // map into address space  shm_status* p = shmat(id_mem, NULL, 0);  if (p == -1)  {  FATAL(...);  }  ...  // lock the memory for synchronization  // do some book-keeping with the control structure  return p->mem; // return user memory } 