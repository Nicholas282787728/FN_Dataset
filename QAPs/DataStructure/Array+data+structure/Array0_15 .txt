Why do array indices start with 0 (zero) in many programming languages?Simply: counting is like this "how far is this element from the 1st elemnt in the array" in other words the offset, so "How far is the 1st element from itself ? Zero" this is simply why. But originally this comes from memory addressing since in memory you move from a base address a certain amount which is the offset. The same principal 