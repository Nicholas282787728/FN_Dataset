Why do we find the height of a binary tree?first height of binary tree code:: /*The tree node has data, left child and right child struct node { int data; node* left; node* right; }; */ int height(node * root) { int l,r=0; if(root==NULL) { return -1; } else { l = height(root->left); r = height(root->right); } if(l > r) return (l+1); else return (r+1); } use of hight of binary tree:: tree traversal if we know the hight than find numbers of node in tree. 36 Views