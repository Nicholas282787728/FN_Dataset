How do I implement a tree in java in which one parent node has multiple (more than 2) children or leaves?The proper way to implement a general tree is to use the “first child-next sibling” model which allows us to implement a general tree (where each node can have an undefined number of child nodes) as a binary tree. See for example : Reconstructing Binary Trees; General Trees 147 Views · Answer requested by 1 person