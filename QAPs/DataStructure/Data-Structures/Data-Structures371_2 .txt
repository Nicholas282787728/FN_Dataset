Do functional programming languages produce much garbage as they favour immutable data structures?Usually? Yes. But — in cases where it actually has any impact on performance, you can often optimize it away. For example, declaring an object to have dynamic extent can ensure that it’s allocated on a local stack frame, destroyed silently, and never has to enter the heap. 345 Views · View Upvotes