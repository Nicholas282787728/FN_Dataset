Are structures in C-language similar to objects in Java?1. Access to data members and member functions can be restricted in java using access specifiers. 2. Java classes are reference types whereas C structs are value types. 3. Java classes can have methods to operate on data. 4. Java classes supports polymorphism. 5. Java classes support inheritance. 6. A base class reference variable can be assigned to the address of a derived class object. 80 Views