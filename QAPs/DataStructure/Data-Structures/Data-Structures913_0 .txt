How many data structures does R language have?Factor is another type of data structure in R. Factors are vectors of categorical variables designed to group the components of another vector with the same size. Below are the properties - 1. One-dimensional values 2. Numeric, character or logical values 3. But only useful for character values  -- Internal storage via levels → memory efficient  -- Levels can be ordered → ordinal variables Last but not least, another type of data structure can be Functions. Functions are objects created by the user and reused to make specific operations. 26 Views