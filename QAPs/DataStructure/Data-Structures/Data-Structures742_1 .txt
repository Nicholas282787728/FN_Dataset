How do I implement a tree in java in which one parent node has multiple (more than 2) children or leaves?Same way as you do with 2 children……each node has a reference to parent and an array of references (arraylist or some list) for their children. Are you looking for Balancing the tree as data is coming in? Can be done easily because a reference can always be overwritten in Java….. 92 Views