Have you ever coded a real life application solely using linked lists?This happens sometimes in functional languages, where linked lists are the primary data structure for representing ordered lists, just like an array is the primary data structure for this in a procedural language. A simple application that doesn't need anything more than just lists and records could thus be said to be implemented “solely using linked lists”. 245 Views