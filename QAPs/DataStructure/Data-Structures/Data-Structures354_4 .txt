Why is it quicker (O1) to find an item in a list through hashing (hashmap), than it is do go directly to the item that you're looking for?For clarification: Why is accessing an item in a python3 list, by index slower than accessing one in a dictionary, by key?Why is accessing an item in a python3 list, by index slower than accessing one in a dictionary, by key? Since Python lists are implemented as arrays of pointers to objects, it is very unlikely that they would be slower than accessing a dictionary entry. 39 Views