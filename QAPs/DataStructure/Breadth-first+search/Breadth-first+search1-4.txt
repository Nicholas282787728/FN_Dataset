How does breadth-first search (BFS) work?With a queue? An intuitive explanation assuming no any prior knowledge in data structure would be greatly appreciated.
First you have to choose data structure to represent graph, there are two ways adjacency list and adjacency matrix, I am using adjacency list for BFS. Pseudocode goes like this: BFS(G,s)// s is the root vertex for ∀  v ∈  G.V        v.color = white;        v.d = -\infnty  ; // v.d represent distance        v.p = nil; // v.p represent parent of v s.color = grey; s.d = 0 ; s.p = nil; Enqueue(Q,s) while(Q!=nil)     u = Dequeue(Q);      for(∀  v in Adj[u])          if(v.color==white)               v.color = grey;               v.d = u.d + 1;               v.p = u;               Enqueue(Q,v); u.color = black; Updated 17 Dec 2014 • View Upvotes