What is the best (programming) algorithm that you have ever created?Anything that you're proud of and would like to share the algorithm as well?
I wrote a program to find the powerset of a given set using the concept of binary numbers. Though not extraordinary, was happy doing it.   Algorithm -   Suppose the set is - {1,2,3}   So here n is 3 as we have 3 elements.   I run a loop from 0 to 2^n -1 and get the binary equivalent i.e 000 to 111 in this case.   Now mapping each binary number to the index of the elements in the set above, I pick up the element if corresponding index is 1, discard otherwise.     Here is the code:     #include<stdio.h> #include<alloc.h> #include<math.h>   int* bin(int n,int x) { int *a; a=(int *)malloc(n*sizeof(int));   while(x!=0) { if(x>=(pow(2,n-1))) { a[n-1]=1; x=x-(pow(2,n-1)); } else { a[n-1]-0; }   n--; } return a; }   void main() { int n,j,k=0,b[1500]; long i; int *a; int *c; printf("Enter n\n"); scanf("%d",&n); a=(int *)malloc(n*sizeof(int)); c=(int *)malloc(n*sizeof(int)); printf("Enter the elements\n"); for(i=0;i<n;i++) { scanf("%d",&a[i]); }     for(i=0;i<pow(2,n);i++) { c=bin(n,i); for(j=0;j<n;j++) { if(c[j]==1) { b[k++]=a[j]; } } b[k++]=0; }     for(i=0;i<k;i++) { if(b[i]==0) printf("\n"); else printf("%d",b[i]); } } Updated 2 Oct 2013 • View Upvotes