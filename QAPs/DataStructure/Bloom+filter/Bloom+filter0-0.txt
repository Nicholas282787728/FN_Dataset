What are the best applications of Bloom filters?
Oracle uses Bloom filters to perform Bloom pruning of partitions for certain queries. For example, when joining a date dimension table with a large fact table partitioned by date, a Bloom filter can be built over the date dimension and used to prune partitions from the fact table [1]. [1] http://structureddata.org/2010/1... 