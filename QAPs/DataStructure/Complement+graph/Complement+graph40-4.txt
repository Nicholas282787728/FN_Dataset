What is a Fibonacci Heap?This is a follow-up question to What is the "heap property"?.
I think the fibonacci heap has a major advantange is mergeable. This feature made it useful under some requirement. Other operations have no advantage than binary heap. And its time complexity is amortized. 