As of August 2012, what is the current state of the art in distributed hash tables?
[2013-06-05 Update: Amazingly, this was down-voted. The thought police are out in force again.] Coherence is a widely deployed (since 2001) commercial DHT implementation from Oracle: * Generally deployed in a data center on anywhere from a small number to hundreds (and to date, not more than a few thousand) servers for a single DHT, supporting multi-TB data sets and tens of millions of TPS * Single hop primary key access with ~1/10ms latency (on Oracle Exalogic systems) * Linear scalability of capacity and throughput with constant latency * Parallel queries (including some SQL support) wi... (more)