How do I implement a multi-threaded hash table?
In all multi-threaded problems the worry isn’t reading, it is the simultaneous writing of data causing problems. There are multiple ways around this: Write locks Exclusive write access to one thread and have other threads put changes into a queue to be consumed. Semaphores etc. 469 Views