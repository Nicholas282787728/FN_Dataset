What do you mean by Hash Map and Hash Table?
First Of all I want to show you the different between Collection Interface and MAP Interface and when we should go for which one?? Map Interface :- when we should go for map “when we want to represent a group of object into a single unit in the form of (Key and value ) pair then we should use ” Example :- (KEY , VALUE) pair <ROLL NO , NAME> , <IP ADDRESS , DOMAIN NAME> and each (KEY , VALUE) pair is called ENTRY. the we should go for Map Interface !! Some Map Specific Method using that we will done operation over Map….. Map<Integer,String> map=new HashMap<Integer,String>();... (more)