How do hash tables work? How do distributed hash tables work?
You way you use a hash table is that it takes in the name of something and gives you back that thing (if it exists).  The goal is that you want to be able to find something very quickly, without having to look through every other thing in your collection.  So you have a "hash function" that garbles the name of the thing you're looking for into one specific place for it, and that's the only place you have to look.  Because it's all garbled up you probably won't have two things in the same place even if their names are similar - if it does randomly happen though, there are a few ways to deal ... (more)