How is a hash table different to a linked list or an array?
Let’s take a practical example. Say you’ve got a book of contacts, names, telephone numbers, addresses. You’ve just written them as you met those people, not sorted alphabetically. Now you want to store these in a computer so you can more quickly just type the guy’s name and his info pops up: First you make a record structure to group all that info into one item you can more easily use. The record has fields types as string for name, tel & addr. So now you choose the data structure you wish to collect all these contact records: Array Each record is placed one next to the other in RAM. So you kno... (more)