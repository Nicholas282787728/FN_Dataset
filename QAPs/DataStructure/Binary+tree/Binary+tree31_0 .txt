Iteratively find the size of largest BST subtree in a given binary tree?One way to do this would be to do an in order traversal of tree and then find the longest increasing sequence in the in order traversed data set.... 