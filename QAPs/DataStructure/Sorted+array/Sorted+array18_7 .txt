What's the best way to delete duplicates from a sorted array in time and space?You can do in O(n) time and O(1) space , by taking an integer(or double variable depending on size) and using it as your hash table .Initialize variable to ZERO Traverse the array , in one pass, if any number n comes , set the nth bit in integer. If the bit is already set, that means number n is repeated 