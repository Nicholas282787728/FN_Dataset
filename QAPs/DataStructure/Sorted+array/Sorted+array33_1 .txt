Given two sorted arrays in ascending order, with the same length N, what is the Kth min a [i] +b [j]. time complexity O(N)?该网页不存在附加信息...I did not go through the paper but this should work in O(k) time and O(1) space  1 2 3 4 5 6 7 8 9101112131415161718192021222324252627 int kthMin(int *arr1, int *arr2, int n, int k) { 	//assuming the inputs are valid such as k never exceeds 2n-2 	int i = 0; //counter for arr1 	int j = 0; //counter for arr2 	while(i + j != k) 	{ 		if( i == n - 1) //hit the end of first array 		{ 			j++; continue; 		} 		if( j == n - 1) //hit the end of second array 		{ 			i++; continue; 		} 		if( arr1[i+1] - arr1[i] < arr2[j+1] - arr2[j]) 		{ 			i++; 		} 		else 		{ 			j++; 		} 	} 	return arr1[i] + arr2[j]; } 