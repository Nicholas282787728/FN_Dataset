★What is wrong with the listed regular expression for use with an IP address?
// javascript[code js] // for valid ip addresses; I pass this variable to the below function when appropriate var validRegExPattern = "^(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\\.){3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)$"     function isValidInput(userInput, validRegExPattern)     {         // modifiers: i = case insensitive, g = global, multiline matching         var isValid = new RegExp(validRegExPattern, "gi");         //alert(isValid.exec(userInput));         if (isValid.exec(userInput) === "" || isValid.exec(userInput) === "null" || isValid.exec(userInput) === null ||             isValid.exec(userInput) === false || isValid.exec(userInput) === "false" || isValid.exec(userInput) === 0 ||             isValid.exec(userInput) === "0" || isValid.exec(userInput) === [] || isValid.exec(userInput) === {} ||             isValid.exec(userInput) === "empty string" || isValid.exec(userInput) === "no" || isValid.exec(userInput) === "FALSE" ||             isValid.exec(userInput) === 0.0 || isValid.exec(userInput) === NaN || isValid.exec(userInput) === Number.NEGATIVE_INFINITY) {             return false;         } else {             return true;         }     }[/code] The above is what I changed my function to based on your input and suggestions.  Thanks everyone.  This works really well. Please note I used a triple "=" b/c we shouldn't be type casting values here.