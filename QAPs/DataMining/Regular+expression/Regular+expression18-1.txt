★Are regular expressions concepts generic?Or the concepts differ for different programming languages? I want to master regular expressions in Python. I need some guidance. Thanks.
The base regular expression syntax remains same for all languages. That is, w+ will always match to alphanumeric characters in all languages (except some older versions of Perl which have older regular expression versions). But,  the methods regarding regular expressions are different in various languages. For example, in Python to use regular expressions you have to import it. 1
 import re And about learning regular expressions in Python, first learn it from here (it's for Javascript but you need to learn the core concept first) You Don't Know Anything About Regular Expressions: A Complete Guide - Tuts+ Code Tutorial And then check out the Python re module documentation 7.2. re - Regular expression operations - Python 2.7.9 documentation    Embed Quote