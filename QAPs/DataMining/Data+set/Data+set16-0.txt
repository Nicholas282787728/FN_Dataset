What is the SQL code to transform a data set?
In MySQL, this is a classic GROUP_CONCAT application. The power of MySQL's GROUP_CONCAT Other DB's such as Postgres have functions that can be cobbled together to behave similarly: Postgresql GROUP_CONCAT equivalent?    Embed Quote 