★Why can the piece-wise linear function (e.g. rectifier) learn nonlinear mapping in artificial neural networks?
The rectifier itself is nonlinear (its shape is like this: _/), so the combination of rectifier units will  therefore be nonlinear too.    Embed Quote